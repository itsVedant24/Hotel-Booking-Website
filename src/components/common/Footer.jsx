function Footer() {
  return (
    <footer style={styles.footer}>
      <p></p>
    </footer>
  );
}

const styles = {
  footer: {
    textAlign: "center",
    padding: "10px",
    background: "#eee",
    marginTop: "40px",
  },
};

export default Footer;

